<!-- https://www.playframework.com/documentation/latest/SettingsLogger -->
<configuration>

    <conversionRule conversionWord="coloredLevel" converterClass="sz.scaffold.tools.logger.ColoredLevel"/>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/application.log</file>
        <encoder>
            <pattern>%date [%level] from %logger in %thread - %message%n%xException</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%date{HH:mm:ss.SSS} %coloredLevel %logger{15} in [%thread] - %message%n%xException{10}</pattern>
        </encoder>
    </appender>

    <appender name="ASYNCFILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
    </appender>

    <appender name="ASYNCSTDOUT" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="STDOUT"/>
    </appender>

    <root level="WARN">
        <!--<appender-ref ref="ASYNCFILE"/>-->
        <appender-ref ref="ASYNCSTDOUT"/>
    </root>

    <logger name="App" level="DEBUG"/>

    <!--tushare-->
    <appender name="tushare_file" class="ch.qos.logback.core.FileAppender">
        <file>logs/tushare.log</file>
        <encoder>
            <pattern>%date [%level] in %thread - %message%n%xException</pattern>
        </encoder>
    </appender>

    <appender name="tushare_asyncfile" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="tushare_file"/>
    </appender>

    <logger name="tushare" level="DEBUG" additivity="false">
        <!--<appender-ref ref="tushare_asyncfile"/>-->
        <appender-ref ref="ASYNCSTDOUT"/>
    </logger>

</configuration>
